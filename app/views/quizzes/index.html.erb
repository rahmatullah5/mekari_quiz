<p id="notice"><%= notice %></p>

<h1>Quizzes</h1>

<table>
  <thead>
    <tr>
      <th>Level</th>
      <th>Hint images</th>
      <th>Hint</th>
      <th>Cover</th>
      <th>Answer</th>
      <th>Point</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @quizzes.each do |quiz| %>
      <tr>
        <td><%= quiz.level %></td>
        <td><%= quiz.hint_images %></td>
        <td><%= quiz.hint %></td>
        <td><%= quiz.cover %></td>
        <td><%= quiz.answer %></td>
        <td><%= quiz.point %></td>
        <td><%= link_to 'Show', quiz %></td>
        <td><%= link_to 'Edit', edit_quiz_path(quiz) %></td>
        <td><%= link_to 'Destroy', quiz, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Quiz', new_quiz_path %>
